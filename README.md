# 3D_Flip_Card_project
Create a 3D flipping profile card that flips when the user hovers or clicks on it. The front shows a user's image and name, and the back shows dynamically generated information (e.g.. a random fun fact or quote) using JavaScript.
